---
workflow: general-video
flow: automation
storyboard: no
message: "글로만 존재하던 법령이, 누가·무엇을·언제가 보이는 구조도가 된다"
destination: deck-embed
aspect: 1920x1080
language: ko
audience: 국무총리·정부 고위 인사 (2026-09-02 발표)
length: 58s
angle: transformation
---

## Intent

국무총리 발표 덱(제도100 소개 → 광주 반도체 클러스터 행정절차 조사)의 2장
"제도 모델" 자리에 삽입하는 30초 무음 모션그래픽. 제도100을 만든 취지 그
자체를 영상 한 편으로: 법령은 글로 존재하고, 절차(누가·무엇을·언제)는 문장
속에 숨어 있다 — 제도100은 그것을 구조도로 만든다. 행사장 음향에 의존하지
않도록 완전 무음 + 화면 자막 기반. 마지막에 "이렇게 만든 제도 지도 580여 개"
스케일 아웃으로 닫는다.

## Assets

- ~/korea100/web/data/institutions/environmental-impact-assessment.json —
  실데이터 소스: 스윔레인 레인·게이트·노드(주체/행위/법조문). 영상 속
  구조도는 이 데이터의 실제 구조를 따른다 (지어내기 금지 — korea100 정직성
  원칙).
- 법령 조문 원문: 환경영향평가법 실제 조문 텍스트 (law.go.kr DRF, OC=test로
  빌드 전 취득해 정적 자산으로 굽는다 — 렌더 시 네트워크 접근 금지).

## Customizations

- 도입부(0–8.2s, 2026-08-26 추가): "직제 개정 협의 결과를 시달받았다" 슬램 +
  행정용어 칩 8종 → "AI로, 행정 리터러시 — AI가 행정을 국민의 언어로 번역합니다"
  리빌 → 조문 벽으로 크로스페이드.
- 4단계 변환 아크: ①글(조문 벽) → ②문장 속 주체·행위·기한 하이라이트 →
  ③조각이 카드로 응축돼 레인으로 정렬, 엣지 드로잉 → ④줌아웃: 1장이 수백
  장 그리드가 되며 "580여 개" 카운터.
- 챕터 5(38.4–48.4s, 2026-08-26 추가): "제도끼리 모아보면?" — 반도체
  클러스터 제도 칩 12종 수렴 + 1,281 카운터 + 게이트 8·마일스톤 49·제도 103.
- 챕터 6(48.4–58.2s): "손에 잡히는 AX" — AS-IS 6단계 중 3단계 취소선(개념도
  명시) vs TO-BE 3단계(AI 배지) 전후 비교 → "실전 AX 가이드북으로" 다크 카드
  클로징.
- 마지막 프레임은 덱 다음 슬라이드로 자연스럽게 이어지도록 정적 홀드 1.5초.

## Notes

- 무음(오디오 트랙 없음). 자막은 화면 타이포로 직접.
- 팔레트는 korea100 DESIGN.md 그린 톤(#0f9f72 계열, 조용한 정책 데스크)과
  일치시킬 것 — 덱과 사이트와 같은 세계관으로 보여야 함.
- 한국어 폰트는 로컬 자산으로 임베드(렌더 환경 폰트 의존 금지).
- 총리 청중: 화려함보다 정확·절제. 과장 수치·가짜 진척 표시 금지.

## Render note (2026-08-27)

- hyperframes 0.8.15 렌더러가 약 46초 이후 클립을 캡처하지 못한다(스냅샷·프리뷰·
  Studio는 정상, 워커 수 무관). 그래서 **챕터 분할 렌더 + concat** 구조로 만든다.
- `index.html` 이 유일한 원본. 타임라인 맨 위 `const T = {...}` 가 챕터 기준 시각이고,
  모든 트윈은 `T.<챕터> + 오프셋` 으로만 위치를 잡는다.
- `python3 tools/split-chapters.py` → `/private/tmp/lawmorph-chapters/<챕터>.html` 12개 생성
  (대상 챕터를 t=0 으로, 나머지 클립·챕터 상수는 900초로 보냄). 각 챕터는 46초 미만이어야 한다.
- `bash tools/render-all.sh` → 챕터 파일을 index.html 자리에 잠시 복사해 렌더한 뒤
  `renders/lawmorph-final.mp4` 로 이어붙임(원본은 trap 으로 항상 복구).

### 이 구조가 이렇게 된 이유(다시 밟지 말 것)
- `render -c compositions/x.html` 은 **화면이 통째로 빈다**: 페이지 URL 이 /compositions/ 라
  `assets/*.js` 가 404 → 데이터 미로드 → 빌더가 throw → 타임라인 미등록 → 전 클립 숨김.
  compositions/ 안에 assets 를 복사해도 동일. 그래서 index.html 자리 교체 방식을 쓴다.
- 프로젝트 안에 루트 컴포지션이 둘 이상이면 `lint: multiple_root_compositions` 로 막힌다.
  그래서 챕터 파일은 프로젝트 밖에 쓴다.
- 스플리터 정규식은 `id=` 를 `(?<![-\w])id=` 로 잡아야 한다. 안 그러면
  `data-composition-id="main"` 이 걸려서 **루트 자체가 900초로 파킹**돼 빈 영상이 나온다.
- 데이터는 전부 `python3 tools/gen-assets.py` 가 korea100 원본에서 생성한다
  (assets/data.js · cases.js · montage.js · megadetail.js). 숫자를 손으로 쓰지 않는다.
- Studio 프리뷰를 열면 index.html DOM 에 data-hf-id 가 스탬프된다 — 이후 스크립트
  편집은 리터럴 문자열 매칭 대신 id 기반 정규식으로.

## 2026-08-29 추가 수정 2차 — 브리핑 예시 슬라이드 + 3부 문구

- **brief 챕터 신설(185.0s, 13.0s)** — loop 다음. 좌: 설명 3카드(관문 ID로 말한다 / 상태 전환을 제안한다 / 확정과 신호를 구분한다), 우: 텔레그램 메시지 목업. 목업 내용은 지어내지 않고 `~/korea100/web/public/warroom/loop/briefing.txt` **2026-08-28 실제 발송분을 발췌**(관문 ID·수치 그대로)
- 3부 크레딧: "한국행정연구원 공동연구" → **"with 한국행정연구원"**, 폰트 축소(`.pk .pw` 17px, 본문 22px 대비 작게)
- 자막 sub20: "2부가 제로베이스라면, 3부는 재개발" → **"2부가 문샷이라면, 3부는 대형 재개발"**. p3sub도 문샷/대형 재개발 비유로 통일
- 이후 전체(part3~close) +13.0s 이동, 전체 길이 272.6s → **285.6s**(18챕터). 내레이션 n22c 추가 + n23~n31 +13.0s

## 2026-08-29 추가 수정 — 루프에 텔레그램 브리핑 스텝

- loop 챕터 8부: 6단계(기사→AI판별→후보발굴→사람검증→재계산→**텔레그램 브리핑**)로 확장, 11.0s→17.0s
- 일반 기사 스크래핑 봇과 구분: "기사 요약이 아니라, 절차 진행상황까지 읽어서" 명시 (ls5 카드 + 자막 subclip19b + 내레이션 n22b)
- 이 챕터 이후 전체(part3~close) 시각을 +6.0s 밀어냄. `T` 상수·각 섹션 `data-start`·`SUBS` 배열·`narration.json`의 n23~n31 `at` 값 전부 +6.0 일괄 이동, 전체 길이 266.6s→**272.6s**
- `tools/split-chapters.py`: subclip 번호가 항상 숫자라고 가정한 정규식이 "subclip19b" 같은 비숫자 접미 id에서 깨짐 → `re.fullmatch(r'subclip(\d+)', elid)` 로 방어. 새 서브클립처럼 숫자가 아닌 id를 쓸 경우 CHAPTERS의 해당 챕터 `clips` 리스트에 id를 직접 추가해야 함(안 그러면 900초로 파킹됨)
- 신규 요소의 GSAP 트윈은 반드시 챕터 변수(`LP + offset`) 기준으로 작성 — 절대 초(178.45 등)로 쓰면 챕터 분할 렌더 시 해당 청크의 0~dur 구간 밖이라 안 뜬다(이번에 실수했다가 바로잡음)

## 2026-08-29 3차 개편 — 2부↔3부 교체 + 다중 프로젝트·부처 상황판

사용자 지시: ①2부와 3부를 맞바꾼다 ②새 3부(거시)가 반도체 하나로 끝나지 않게,
나머지 3개 메가프로젝트를 같은 방식으로 보여 "여러 프로젝트로 확장 가능"을 함의로 남긴다
③마지막에 부처 상황판 시연 슬라이드를 더한다 ④행정 문샷의 의의를 더 설명한다.

- **부 순서 교체** — 1부(제도를 읽다) → **2부 미시·실증(AX)** → **3부 거시·문샷**.
  `T` 상수의 값 순서가 곧 재생 순서다. DOM 순서는 건드리지 않았다(프레임워크가
  `data-start` 로만 순서를 잡고, 자막 클립이 DOM 끝에 있어 항상 위에 쌓인다).
  기존 챕터 id(`stage-part3`·`stage-close` 등)는 그대로 두고 문구·시각만 바꿨다 —
  id 를 바꾸면 split-chapters·GSAP 셀렉터가 전부 따라 깨진다.
- **문샷 의의 확장(moon 10.4s → 20.0s)** — 규모 비유(10개/1만개/1억개) 뒤에
  "전제 하나가 달라졌다 → OpenAI·Anthropic·xAI 토큰 가격 인하 → 값이 계속 내려가면
  인건비가 거의 들지 않는 직원을 원하는 만큼 두는 것과 같다" 논거를 붙이고,
  "지금 인력으로는 감당이 안 돼 접어뒀던 일을 다시 꺼내 볼 수 있다"로 착지한다.
  **구체 인하율·가격 수치는 쓰지 않는다**(검증 불가 — korea100 정직성 원칙).
  규모 비유와 토큰 논거는 높이 268px 고정 컨테이너(`#moonstage`)에서 교대시켜
  세로 레이아웃이 흔들리지 않게 했다.
- **신규 챕터 4개** — `p3`(3부 시작) · `multi`(다중 프로젝트) · `board`(부처 상황판) · `fin`(최종 클로징).
  기존 `close` 는 2부 마무리로 남기고 로고 락업만 `fin` 으로 옮겼다.
- 전체 285.6s → **349.2초 (22챕터)**, 자막 33개, 내레이션 43문장.

### 2026-08-29 4차 — 실사이트 시연 2건 추가 (mdemo · bdemo)

사용자 지시: 부처 상황판도 반도체처럼 Playwright 로 실제 홈페이지를 띄워 시연하고,
5극3특 시연 영상도 추가한다. 349.2s → **389.2초(24챕터)**.

- **가짜 패널 목업 제거** — `board` 우측에 있던 `#bdpanel`(부처 막대 6줄 + 레버리지 카드)을
  통째로 버리고, 실제 화면은 `bdemo` 녹화가 맡는다. 목업과 실화면이 같은 프레임 안에서
  다른 숫자를 말하게 두지 않는다.
- **관문 수 표기 통일** — 실제 부처 상황판 HUD 는 `GATES 149` 로 뜬다. 이건
  `totals.gates`(=부처별 소관 수의 합 91, **공동소관 중복 계상**) + `unassignedGates`(58) 이다.
  서로 다른 관문의 실제 수는 **135**(4개 사업 노드 합)이므로, 설명 슬라이드는
  `MULTISTATS.totalGates` 를 써서 **18부처 / 135관문 / 58 소관 미특정**으로 적는다.
  91 은 화면에 쓰지 않는다.
- **레버리지 문구** — `maxLeverage.blocked` 는 그 관문의 **경성(hard) 하류 전이 폐포 크기**다.
  "막고 있다"가 아니라 "뒤로 N개 관문이 걸려 있다"로 적는다(실화면의 '쥐고 있는 것'은
  frontier 목록이라 다른 수치가 뜬다 — 다른 지표임을 문구로 구분).
- `tools/render-all.sh <챕터…>` — 지정 챕터만 다시 렌더하고 나머지는 기존 파트를 재사용해
  concat 한다. 챕터는 각자 t=0 부터 독립 렌더라, 뒤 챕터 시각이 밀려도 픽셀은 그대로다.
- `tools/split-chapters.py` — 시연 클립이 여럿이 되어, 소스에서 `<video>` id 를 뽑아
  **그 챕터에 속하지 않는 비디오 태그를 전부 제거**하도록 일반화(파킹된 비디오가
  커버리지 게이트에 걸려 렌더가 중단되는 문제).

### 2026-08-29 5차 — 사례 정리 + 문샷 개념도

사용자 지시: ①mdemo 를 12~14초로 ②2부 사례는 여비몬만 ③1부에서 소상공인 정책자금 빼고
환경영향평가–공장설립승인을 이어서 ④행정 문샷 내레이션 싱크 + XY 그래프.
389.2s → **345.2초(23챕터)**.

| 변경 | 전 | 후 |
| --- | --- | --- |
| `mont` 소상공인 삭제, 공장설립승인 한 건 | 19.0s | 10.0s |
| `ax2` 삭제(세종사이렌·눈치코치) | 25.0s | — |
| `moon` XY 개념도로 재구성 | 20.0s | 19.0s |
| `mdemo` 5극3특 시연 단축(영상 21.2→12.2s) | 22.0s | 13.0s |

- **문샷 개념도** — 텍스트 3줄(토큰 칩·문장) 대신 SVG 그래프. X축 시간, 곡선 둘:
  **토큰 단가↓(호박)** · **같은 예산으로 감당할 수 있는 업무량↑(초록)**, 벌어지는 밴드를
  "지금까지 접어둔 일"로 채운다. **눈금 숫자는 넣지 않고 "개념도" 배지를 단다** —
  실제 가격 곡선 데이터가 없으므로 눈금을 그리면 없는 수치를 주장하게 된다.
  곡선은 `getTotalLength()` 로 dasharray 를 잡아 draw(seek 해도 같은 그림).
- **내레이션 싱크** — 이전 판은 내레이션이 화면보다 3~7초 늦었다(토큰 문장이 화면에 뜨고
  한참 뒤에 말이 나옴). n16/n16b/n16c/n16d 를 화면 비트(MN+0.6 / +6.3 / +9.7 / +12.9)에
  직접 못 박았다. **새 비트를 넣을 때 narration.json 의 at 도 같이 옮길 것.**
- `ax2` 를 빼면서 도입부 "전임자 머릿속" 회수가 사라져, `ax3` 내레이션(n30)에
  "세종사이렌도, 도입부의 그 전임자 머릿속 문제를 푼 인수인계도 같은 방식"을 넣어 회수한다.
- `cases.js` 는 3건을 그대로 두고, 보드 빌더가 **`#case<i>` 가 있는 사례만** 만든다.
- **함정**: `subclip19b`·`subclip19c` 는 비숫자 id라 `SUBS` 배열에 없고 챕터 변수(LP/BR)로
  움직인다. 전체 재타이밍 스크립트가 이 둘을 빠뜨려 자막이 겹쳤다(check 가 잡아냄).
  일괄 이동할 때 **숫자 자막 + 이 둘**을 함께 옮길 것.

### 2026-08-29 6차 — 5분 이하로 (299.8초)

사용자 지시: 허가·신고는 살리고, 광주 시연도 살리고, 5극3특 시연을 더 줄여
총 5분 이하로. 텔레그램 브리핑은 유지. 345.2s → **299.8초(21챕터)**.

| 조치 | 절감 | 재렌더 |
| --- | --- | --- |
| `intro` 에서 업무편람·전임자 덩어리만 삭제 (35.6→28.4) | −7.2 | intro |
| `mont` 삭제 — 공장설립승인 몽타주 | −10.0 | 없음 |
| `p3` 삭제 — 3부 오프너 | −10.0 | 없음 |
| `mdemo` 13.0→9.0 (영상 8.2s 재녹화) | −4.0 | mdemo |
| `bdemo` 21.0→13.0 (영상 12.2s 트림) | −8.0 | bdemo |
| `part2`·`close`·`fin` 페이싱만 조임(문구 유지) | −6.2 | 3챕터 |

- **`p3` 를 뺀 근거** — `close` 마지막 줄이 이미 "여기까지가 2부 — 이제 3부, 판을 키웁니다"라
  3부 오프너가 겹쳤다. 챕터 삭제는 concat 목록에서만 빠지므로 재렌더가 0이다.
- **5극3특 시연 재녹화** — `pmbrief` 같은 **시나리오 칩이 켜진 상태에서 관문을 클릭하면
  절차 패널이 열리지 않는다**(엣지 하이라이트만 됨). 필터 없는 기본 화면에서 바로
  N12(절차 4건)를 열도록 스텝을 바꿔야 절차 체인이 화면에 잡힌다.
- **어두운 챕터의 페이지 헤더** — `#bg .meta`·`.toprule` 은 moon·demo·mdemo·bdemo 에서
  불투명 배경/영상에 가려 이미 안 보이지만, 상태를 명시하지 않으면 대비 검사가
  실제 화면과 다른 판정(2.7:1 error)을 낸다. `tl.set` 으로 해당 구간에 내렸다 올린다.
- `assets/montage.js`·`fetch-laws.py` 와 `cases.js` 3건은 지우지 않는다. 빌더가
  **자리(`#mont-host`·`#case<i>`)가 있을 때만** 만들도록 해서, 되돌릴 때 마크업만 살리면 된다.
- 트리밍은 `record-*.mjs` 의 `trim` 옵션으로 굽는다 — 스크립트 밖에서 자르면
  재녹화 때 길이가 원래대로 돌아와 챕터가 깨진다.

## 구성 (299.8초 · 3부 21챕터, 2026-08-29 6차)

| 챕터 | 길이 | 내용 |
| --- | --- | --- |
| open | 4.6s | 표지 — 덱 표지와 동일(국무총리 보고 · 참여혁신조직실), 무내레이션 |
| intro | 28.4s | "행정은 일반 국민에게 어렵습니다" 슬램 → 허가/신고/수리 3카드 → 시달·고시 → "행정 리터러시가 필요합니다" 리빌 (업무편람·전임자는 6차에서 삭제) |
| doc | 14.2s | 환경영향평가법 현행 조문 + 주체·행위·기한 하이라이트 |
| map | 16.0s | 스윔레인 조립(9주체×8게이트, 절차 18) |
| reg | 9.0s | 제도 타일 그리드 + 카운터(K100.instCount, 현재 647) |
| auto | 8.6s | 자동화된 증가 — 요청 메일→AI 에이전트 초안→사람 검증→등재 4스텝 |
| part2 | 8.0s | 전체 3부 구성 로드맵 — **2부(미시) 하이라이트** |
| **— 2부 : 미시로, 실증으로 —** | | |
| part3 | 12.0s | 2부 시작 — 질문의 전환("맡겨야 하는가"→"맡기고 있는가"), with 한국행정연구원 |
| ax1 | 22.6s | 여비몬 — 블록 5개가 콜아웃과 함께 AI 레인으로 (20→14). **2부 사례는 이 한 건** |
| ax3 | 15.4s | 우수사례 6건 하반기 심층분석 — 드러커 인용 + 특정·측정·이식 3단계 |
| close | 10.0s | 실전 AX 가이드라인으로 — 현재 연구 중 + "여기까지가 2부" |
| **— 3부 : 거시로, 문샷으로 —** | | |
| moon | 19.0s | (가칭) 행정 Moonshot — 규모 비유 → **토큰 단가↓/업무량↑ XY 개념도** → "수천 명의 AI 공무원" |
| mega | 16.0s | 제도 칩 수렴 + 절차 카운터 + 8게이트 체인 + 상태 필 (전부 MEGASTATS) |
| demo | 25.0s | warroom/map 실사이트 녹화 클립(assets/warroom-demo.mp4, 24s) 재생 |
| loop | 17.0s | 워룸 루프 — 기사→판별→후보→사람 검증→재계산→텔레그램 브리핑 순환 |
| brief | 13.0s | 텔레그램 브리핑 실물 예시(2026-08-28 실제 발송분 발췌) |
| **multi** | 16.0s | **여기까지가 프로젝트 하나 — 4개 메가프로젝트 카드 + 공유 제도 30 + `?p=` 주소** |
| **mdemo** | 9.0s | **5극3특 지도 실사이트 녹화(assets/fivepoles-demo.mp4, 12.2s) — 총리 브리핑·관문 클릭** |
| **board** | 12.0s | **축을 뒤집으면 부처 상황판(설명) — 18부처·관문 135·소관 미특정 58 + 최대 레버리지** |
| **bdemo** | 13.0s | **부처 상황판 실사이트 녹화(assets/ministry-demo.mp4, 19.9s) — 레버리지 정렬·부처 상세·사업 필터** |
| **fin** | 11.0s | **최종 클로징 — 제도 647 / 프로젝트 4 / 관문 135 / 부처 18 + 로고 락업** |

### 2026-08-29 개편에서 추가된 도구·자산
- `tools/fetch-laws.py` — law.go.kr DRF(OC=test)에서 몽타주 샘플 조문 원문 취득 → `assets/laws.json` 캐시 (빌드 전 1회, 렌더 시 네트워크 금지 유지)
- `tools/record-warroom.mjs` — Playwright(chromium)로 warroom/map 실사이트 시연 녹화 → `assets/warroom-demo.mp4` (가짜 커서 주입, N37 클릭→절차 패널→워게임 시나리오)
- `tools/rec-lib.mjs` + `record-fivepoles.mjs` + `record-ministry.mjs` (2026-08-29 3차) —
  녹화 코어(가짜 커서·부드러운 커서 이동·선택자 클릭·webm→h264 변환)를 `rec-lib.mjs` 로 빼고
  시연 스텝만 스크립트별로 둔다. 산출물 `assets/fivepoles-demo.mp4`(21.2s) ·
  `assets/ministry-demo.mp4`(19.9s). `click()` 은 선택자를 못 찾으면 **건너뛰고 로그만 남기므로**,
  녹화 후 반드시 프레임을 뽑아 각 스텝이 실제로 일어났는지 눈으로 확인할 것.
- `gen-assets.py` 확장 — `assets/data.js`(EIA+instNames+instCount)와 `assets/megastats.js`(마일스톤·제도·절차·상태 분포·loopBefore) 생성. 모든 화면 수치는 이 자산에서 JS로 채움
- 자막 27개로 재편(sub-dark 클래스: moon·demo 등 어두운 배경용). 화면 대형 타이포와 같은 문구를 자막으로 중복 표기하지 않는다(사용자 지침)
- CLI 핀 0.8.15 → **0.8.17** (upgrade --check 후 적용, `npm run check` 통과 확인). 챕터 분할 렌더 구조는 유지

### 2026-08-29 3차 개편에서 추가된 도구·자산
- `gen-assets.py` → **`assets/multistats.js`** 신규 (`window.MULTISTATS` · `window.BOARDSTATS`).
  - MULTISTATS: `web/public/warroom/p/index.json` 의 4개 프로젝트를 돌며
    **mega 챕터와 같은 방식**(`web/data/mega-projects/projects/*.json` 의 `templateRefs` →
    제도 원본 `process.nodes` 카운트)으로 관문·제도·절차를 센다. 워룸
    `procedures.json` 으로 세면 광주 절차가 mega 챕터의 1,373 과 어긋나므로 쓰지 않는다.
  - `shared-institutions.json` 에서 공유 제도 30건과 4개 프로젝트 전부에 걸린 제도를 뽑는다.
  - BOARDSTATS: `warroom/map/ministry-board.json` 의 totals·상위 6부처·최대 레버리지.
- 다중 프로젝트 합계(관문 135 · 절차 3,581)는 **프로젝트별 합계라 공유 제도가 중복 계상**된다.
  화면에 그 각주를 반드시 함께 띄운다(`#mtsum .mtn`).
- `tools/render-all.sh` 의 `ORDER` 하드코딩 제거 — `split-chapters.py` 의 `CHAPTERS` 에서
  정규식으로 뽑아 쓴다. 챕터를 더하거나 순서를 바꿀 때 고칠 곳은 `CHAPTERS` 한 군데다.
  렌더 CLI 도 `hyperframes@0.8.17` 로 핀 고정(무핀 `npx hyperframes` 는 최신을 끌어와
  `npm run check` 와 다른 버전으로 렌더될 수 있었다).
- 자막 33개(신규 28~33). 새 자막 id 는 반드시 `subclip<숫자>` 형식이어야
  split-chapters 의 `subclip(\d+)` 매칭에 걸린다.

## 오디오 (2026-08-27 추가)

- HeyGen 미로그인 → 오프라인 경로. **내레이션 = Supertonic 3 (로컬 ONNX, 31개 언어·한국어 지원)**,
  **BGM = MusicGen 로컬 생성**(30초 시드). 현재는 **현악 없는 피아노·마림바·첼레스타 계열**(104bpm 장조).
  후보들을 음향 지표로 비교해 고름 — 중심주파수/온셋:
  ① track-solemn 782Hz·3.1/s(너무 진중) · ② track-baroque 2303Hz·6.4/s(너무 밝음) ·
  ③ track-piano-dark 543Hz·2.0/s(더 어두움) · **④ track 964Hz·4.9/s(채택)**.
  후보는 assets/bgm/ 에 모두 남겨둠. 믹스는 베드 0.12 + 2.2kHz -2.5dB 딥(남성 보이스 대역 회피). 로그인하면 HeyGen 카탈로그 음악·고품질
  TTS 로 교체 가능(`npx hyperframes auth login` 후 audio 단계만 다시).
- TTS 는 `.venv-tts`(python3.13 + `pip install supertonic`), 모델은 `~/.cache/supertonic3`.
  보이스 F1~F5·M1~M5 — 현재 **M1(남성)**, `NARRATION_VOICE` 로 교체. 속도는 엔진 파라미터
  `NARRATION_SPEED`(현재 1.12) 를 먼저 쓰고, 그래도 넘치면 ffmpeg atempo 로 보정한다.
  `audio/samples/` 에 10개 보이스 한국어 샘플이 있다.
- 원고는 `audio/narration.json` (id·at(초)·text 22문장). `.venv-tts/bin/python tools/make-narration.py`
  가 문장별 WAV 를 만들고 at 시각에 배치해 `audio/narration.wav`(190초) 를 만든다.
  슬롯을 넘치면 1.12배 이내로 자동 속도 보정하고, 그래도 넘치면 실패로 알린다.
- `bash tools/mix-audio.sh` → 30초 BGM 을 4초 크로스페이드로 이어 붙여 190초 루프를
  만들고, 내레이션을 asplit 해 한 갈래는 사이드체인 키로 써서 말할 때 BGM 을 누른다.
  마지막에 loudnorm(-16 LUFS) + 리미터. 산출물 `renders/lawmorph-final-audio.mp4`.
- 오디오를 컴포지션이 아니라 이 단계에서 합치는 이유: 챕터 분할 렌더라 컴포지션에
  넣으면 BGM 이 챕터마다 끊긴다.
- 실측 레벨: 내레이션 구간 -18 dB / BGM 단독 구간 -26 dB / 피크 -1.5 dB.

## 2026-08-30 7차 — 브리핑 씬을 텔레그램 말풍선에서 실물 보고서로

사용자 지시: 텔레그램 메시지 슬라이드를 실제 HWPX 보고서 화면으로, 세 장이 겹쳐 보이게.

- `stage-brief`(225.8s·13.0s) 우측 `#briefphone`(다크 말풍선 목업) 삭제 → `#briefstack`.
  8/26·27·28 **실제 생성분**(워룸 파이프라인이 그 날짜로 만든 HWPX→PNG)을 `assets/report-*.png`
  로 넣고 세 장을 왼쪽으로 부채질해 겹친다. 오른쪽으로 펴면 뒷장에서 보이는 부분이
  **여백뿐이라 백지로 읽힌다** — 방향을 왼쪽으로 잡아야 뒷장에서도 본문이 보인다.
- 회전은 바깥 `.rslot` 이 CSS 로, 등장 트윈은 안쪽 `.rpage` 가 받는다. 한 요소에 CSS
  transform 과 GSAP transform 을 같이 걸면 `gsap_css_transform_conflict` 로 lint 가 막는다.
- 좌측 2번 카드 `상태 전환을 제안합니다` → `조치까지 적습니다`(화면이 조치 칸을 보여주므로).
- 자막 `sub19c` 교체: `이슈가 어느 절차에 걸렸는지 짚고, 부처별 조치까지 — 한 장 보고서`.
- 나레이션 `n22c` 도 같은 뜻으로 교체하고 TTS 재생성(82자·10.87s, 13.0s 슬롯에 속도 보정 없이
  들어감). **자막만 바꾸면 음성과 어긋난다** — 이 구간은 원래 '상태 전환 검토'를 말했다.
- 재렌더는 `bash tools/render-all.sh brief` 한 챕터만(나머지 20개는 기존 파트 재사용) →
  `bash tools/mix-audio.sh`. 299.8s 유지. 이전 판은 `renders/lawmorph-final-audio.prev.mp4`,
  이전 내레이션은 `audio/narration.prev.wav`.
