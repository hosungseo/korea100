# 변경 이력 (CHANGELOG)

이 사이트의 모든 공개 변경 사항을 `X.Y.Z` 체계로 기록합니다.

- **X (major)**: 데이터 계약·사이트 구조가 바뀌어 기존 링크나 데이터 해석이 달라지는 개편
- **Y (minor)**: 제도 추가, 새 화면·기능, 콘텐츠 확충
- **Z (patch)**: 오탈자·조문 정정, 버그 수정, 문구·스타일 조정

버전은 `web/package.json`의 `version`과 사이트 푸터에 함께 표시하고, main 머지 시 git 태그(`vX.Y.Z`)를 남깁니다.

---

## v0.4.0 — 2026-07-12

**콘텐츠**
- 제도 6건 추가 (10건 → **16건**): 예정가격 작성, 입찰보증금과 계약보증금, 입찰참가자격 사전심사(PQ), 종합심사낙찰제, 협상에 의한 계약, 지체상금.
- 신규 건은 새 캔버스 스키마(적용 대상·담당자별 제출서류)와 콘텐츠 4대 원칙(누가·서류·기한·사업종류)에 맞춰 작성. 검증 상태는 `needs-review`(출처 확인 배지)로 공개하며 원문 기계 대조 승격은 후속 반영.
- 조사 워크시트를 `sources/batch-2026-07-12/`에 동봉.

---

## v0.3.0 — 2026-07-12

운영자 피드백 반영: 한 장 캔버스 개편과 순차 분류 체계.

**화면·데이터 계약**
- `돈의 흐름` → **`적용 대상`**: 어떤 사업종류·금액구간·요건에 적용되는 제도인지로 내용 전환 (`canvas.applicability`).
- `문서의 흐름` → **`제출서류`**: 담당자(조달업체·수요기관·계약담당공무원·조달청)별 필요서류 구조로 전환 (`canvas.submittedDocuments`).
- `개선점`(reformPoints)·`권한 관계` 블록 삭제 (권한 데이터는 비교 화면 `핵심 기관`에 유지).
- `목적`을 상세 페이지 제목 아래 설명으로 통합하고, 절차 요약과 법령을 **`절차와 법적 근거`** 블록으로 통합.
- 사이트 헤더 영어 부제를 저장소 이름을 따라 `How Did They Do All That Procurement`로 변경.

**분류 체계**
- 분야를 조달 업무 순서(순차 응집도) 기준으로 재편: **등록과 시장 진입 → 발주 준비 → 공고와 입찰 → 심사와 평가 → 계약 체결과 이행 → 사후관리와 제재**. 제도 공개 순번(priority)도 이 순서로 재배열.

**운영·문서**
- `docs/content-principles.md` 신설: 누가·어떤 서류를·언제까지·어떤 사업종류(4대 원칙)를 콘텐츠 완성 기준으로 명문화.
- `law-verify` 러너의 결과 커밋이 브랜치 push 경합으로 실패하던 문제 수정(pull --rebase), 스모크 테스트를 정보성 단계로 완화.
- 검증 대기 중인 배치 2 초안 4건(일반·제한·지명경쟁입찰, 입찰공고)은 `sources/batch-2026-07-12/drafts/`에 보류(공개 빌드 미포함).

---

## v0.2.0 — 2026-07-12

**콘텐츠**
- 파일럿 10 제도 완성: 적격심사낙찰제, 조달요청(중앙조달), 다수공급자계약(MAS), 계약방법 결정, 물가변동으로 인한 계약금액 조정, 우수조달물품 지정, 선금과 대가 지급 7건 추가 (3건 → **10건**).
- 신규 7건의 검증 상태는 `needs-review`(출처 확인 배지)로 공개. 작성은 다중 에이전트 파이프라인(Opus 작성 → 교차 검증 → 수정 → 재검증)으로 진행했고, 조문 인용은 공개 웹 검색 교차 대조를 거쳤다. 국가법령정보센터 Open API 기계 대조(`article-verified` 승격)는 후속 커밋에서 반영 예정.
- 조사 근거 워크시트를 `sources/batch-2026-07-12/`에 공개.

**운영**
- `law-verify` GitHub Actions 워크플로 추가: law.go.kr egress가 차단된 원격 개발 환경 대신 Actions 러너에서 Open API 조문 기계 대조(`verify:articles`)와 법령 전문 캐시(`sources/law-cache/`) 수집을 실행.

---

## v0.1.0 — 2026-07-12

첫 관리 버전. 원본인 **[한 장으로 끝내는 대한민국 제도 100(korea100)](https://github.com/hosungseo/korea100)** 을 포크해 공공조달 도메인으로 전환한 시점부터의 차이를 기록한다.

### 제도100(korea100) 대비 달라진 점

**콘텐츠**
- 대상 도메인 교체: 일반 행정제도 101개 → **공공조달·계약 제도 100개 목표**. 현재 3개 공개(조달업체 등록, 수의계약, 부정당업자 입찰참가자격 제한). korea100 원본 콘텐츠는 `legacy-korea100` 브랜치에 보존.
- 분야 체계 교체: 국정 전반 10개 분야 → 조달 업무 흐름 기준 분야(조달시장 진입과 참여 준비, 계약방법 결정과 입찰 절차, 제재·분쟁·권리구제 등).
- 근거 법령 축 교체: 국가계약법·시행령·시행규칙, 조달사업법, 계약예규, 나라장터 등록규정 등 조달 법령 중심.

**검증**
- 조문 검증 파이프라인을 국가법령정보센터 Open API 기반 **자동 대조**로 운용하고, 화면 표기를 '검증 완료' 대신 **'자동대조 완료'** 로 명시(사람 심사로 오해 방지).
- 노드 단위 검증 표시(VerificationMark) 추가, 현장 검증 대장 화면 정비.

**화면**
- 업무구조도에서 **진행률 어휘(완료·현재·대기) 제거**: 정적 예시 화면이 이용자 자신의 진행 상태로 오해되는 문제를 없애고, 핵심 단계·병목·보완 회귀만 강조하는 표기로 전환. 상태 어휘는 `web/src/lib/node-status.ts` 단일 출처로 통합.
- '같은 기준으로 비교'를 구조 지표(노드·레인·게이트 수) 나열에서 **제도 실질 비교**(무엇을 하는 제도인가, 누가 관여하나, 결정 권한, 근거 법령, 실무에서 걸리는 곳)로 개편.
- 메인 카탈로그 단순화: 검색창 placeholder '제도', 왼쪽 레일을 주제별 바로가기 전용으로, 정렬 토글·중복 필터·분야/노드 열 제거.
- 상단 메뉴 축소(제도 대장·요청하기), 현장 검증 대장은 푸터로 이동.

**운영**
- 배포: GitHub Pages 자동 배포(main 머지 → `.github/workflows/pages.yml`), 라이브 주소를 README에 명시.
- 연락처: 오류·제보 창구를 `wooseongkyun@korea.kr`로 통일.
